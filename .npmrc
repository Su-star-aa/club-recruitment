# .npmrc — npm 配置
# 解决 Windows 环境下依赖安装兼容性问题

# 使用国内镜像（实验室 / 个人电脑通用）
registry=https://registry.npmmirror.com

# 忽略 peer dependency 冲突警告
legacy-peer-deps=true

# 严格校验 node 版本（低于 18 则拒绝安装）
engine-strict=true

# 锁定 lockfile 版本格式（避免不同 npm 版本产生冲突）
lockfile-version=3
